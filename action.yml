name: "Node Lint And Format"
description: "Lints and formats your code automatically using your npm/yarn scripts defined in package.json! Recently Updated."
author: "MarvinJWendt && Beau Williams"
branding:
  icon: award
  color: orange
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.myInput }}
